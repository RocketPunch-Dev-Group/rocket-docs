---
title: "Applying to courses"
description: "How to apply to a training course and share the ones you like."
keywords: ["course application", "enroll", "apply for training", "application history", "share a course", "go to application page", "training institution", "external application", "delete history", "clear all history"]
---

Once you find a program you like, you can apply through its application page. Besides applying, you can also share the posting with someone else.

## Applying

Applications for courses are handled on each institution's own application page. Select `Go to application page` on the course page to go to the page the institution provides, and follow the institution's instructions for how to apply and what is required.

The course page shows information about the institution along with the Capacity, the status, and the Self-payment amount. Courses do not offer Quick Apply, so follow the institution's instructions for how to apply and what the requirements are.

### Checking your course application history

You can find it under `Settings` \> `External Applications` \> `Education Courses`.

- **Recorded automatically:** Selecting `Go to application page` counts as applying, so the course is recorded here.
- **Delete entries:** If you did not actually apply or you no longer need the record, remove entries one at a time with `Delete`, or all at once with `Clear All History`. Deleting an entry does not affect an application you actually submitted on the external site.

## Sharing

Select `Share` to copy the course link and share it with someone else.

## Frequently asked questions

<AccordionGroup>
  <Accordion title="There is a course in my application history that I never applied to.">
    For courses where you apply on an external page, the moment you select `Go to application page` it counts as applying and is recorded automatically in your application history. If you did not actually apply, delete that entry under `Activity` \> `External Applications` \> `Education Courses`; it does not affect an application actually submitted on the external site.

    For details, see [External application history](/en/activity-external-apply).
  </Accordion>
</AccordionGroup>
