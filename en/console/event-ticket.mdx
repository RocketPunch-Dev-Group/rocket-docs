---
title: "Managing event tickets"
description: "How to check and adjust how many of your issued tickets are left."
keywords: ["ticket", "manage tickets", "tickets remaining", "remaining quantity", "adjust quantity", "limited quantity", "capacity", "sold out", "add a ticket", "edit a ticket", "delete a ticket", "discount code", "promo code", "coupon", "free ticket", "paid ticket", "quantity"]
---

`Rocketpunch` \> `Enterprise Services` \> `Events` \> `Events` \> `Manage event` \> `Tickets` is where you manage the tickets you issued.

## Checking how many tickets are left

Tickets with a limited quantity also show how many remain. You can adjust the quantity in `Ticket management`.

{/* TODO(to write: issuing ticket discount codes) */}
