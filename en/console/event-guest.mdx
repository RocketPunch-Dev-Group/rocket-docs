---
title: "Managing event guests"
description: "How to approve registrants and check them in at the venue."
keywords: ["guests", "guest management", "attendees", "registrants", "queue", "approval", "approve attendance", "check-in", "entry", "attendance", "guest list", "registration status", "tickets remaining", "search guests", "hide guests"]
---

Open your event in `Rocketpunch` \> `Enterprise Services` and manage registrations in `Guests and queue` on the event.

## Registration status

| Status | Description |
| --- | --- |
| Pending | A registrant waiting for host approval. |
| Invited | A guest the host invited. |
| Approved | A guest whose attendance is confirmed. |
| Rejected | A registrant whose request was turned down. |
| Not checked in | A guest whose attendance is confirmed but who has not checked in yet. |
| Checked in | A guest who arrived and was let in. |
| No-show | A guest whose attendance was confirmed but who did not come. |
| Canceled | A guest who canceled their attendance. |

If you did not turn on `Host approval required` when creating the ticket, registrations are approved the moment they are made.

## Reviewing guests

The list shows the guest, ticket type, registration time, and registration status. You can search by name or email, filter by status, and sort by registration date.

## Approving

Review the registrants marked `Pending` and approve them.

For an event with `Hide location` turned on, the location is revealed only to approved guests.

## Checking guests in

On the day of the event, let arriving guests in from `Check-in`. For details, see [Managing event check-in](/en/console/event-check-in).

{/* TODO(to write: the steps for checking a guest in with a QR code) */}

## Checking how many tickets are left

Tickets with a limited quantity also show how many remain. You can adjust the quantity in `Ticket management`.

{/* TODO(to write: issuing ticket discount codes) */}

## Hiding guests

Set this under `Permissions and settings` \> `Additional settings` on the event.

- **Hide guests:** Hides the attending guest list so only the host can see it.
