---
title: "환전하기(스왑)"
description: "토큰을 다른 토큰으로 바꾸거나 다른 네트워크로 옮기는 방법을 알려드릴게요."
---

교환

## 환전으로 할 수 있는 일

환전은 보유한 토큰을 다른 토큰으로 바꾸거나, . Web3 용어로는 스왑(swap) ·

같은 토큰을 다른 네트워크로 옮기는 기능입니다 브릿지(bridge)라고 부릅니다.

낼토큰과 받을 토큰을 선택하면 스왑과 브릿지가 한번에(자동으로) 됨

| 항목 | 내용 |
| --- | --- |
| 토큰 바꾸기(스왑) | 토큰을 같은 네트워크에 있는 다른 토큰으로 바꿉니다. |
| 네트워크 바꾸기(브릿지) | 토큰을 다른 네트워크에 있는 같은 토큰으로 바꿉니다. |
| 둘 다 바꾸기 | 표는 삭제 |

<Note>
  환전은 디지털 자산끼리의 교환입니다. 로켓펀치는 원화를 비롯한 법정화폐와 디지털 자산 사이의 환전, 매수, 매도를 제공하지 않습니다.
</Note>

## 환전하기

`월렛` \> `토큰 간 환전` 을 누르고 낼 토큰과 금액, 받을 토큰을 고르면 견적이 표시됩니다.  한 번 표시된 정보는 1분 간 유지됩니다. 1분이 지나기 전에 견적을 다시 받으려면 `⟳` 를 누르면 됩니다.

견적 조건을 변경하려면 환전 비율, 네트워크 수수료, 슬리피지(변동률), 가격 영향, 최소 수령액, 소요시간을 확인할 수 있으며 (따로)

### 수수료(가스비)

환전할 때는 수수료(가스비)가 발생합니다.

낼 토큰이 있는 네트워크의 기본 토큰으로 부담. 예시로 이더리움 네트워크 환전 시 이더리움 필요 솔라나는 솔

를 직접 부담합니다. 낼 토큰이 있는 네트워크의 기본 화폐가 없으면 환전이 진행되지 않습니다.

예를 들어, Solana의 USDC를 다른 토큰으로 환전하려면 SOL이 필요합니다. 자산을 주고 받을 때와 달리 개인 부담이므로, 수수료로 지불할 토큰이 부족하다면 먼저 받아 두어야 합니다.

수수료 지원 네트워크 - 솔라나 베이스 (로켓펀치가 부담) - 가스 스폰서십

- 환전 시에는?
- 이 네트워크에서 거래가 일어날때 발생하는 토큰을 부담(낼 토큰이 솔라나/베이스면 지원 받을 토큰 네트워크는 상관 x)

## 변동률(허용 슬리피지)

환전이 처리되는 동안에도 환율은 조금씩 움직입니다. 그래서 견적으로 본 수량과 실제로 받는 수량이 달라질 수 있는데, 이 차이를 슬리피지라고 합니다. 로켓펀치 화면에는 변동률로 함께 표기됩니다.

변동률은 업체마다 다르며 견적을 받을 때 표시됩니다. 허용한 값보다 변동률이 커지면 환전은 진행되지 않고 취소됩니다. 허용 범위는 `토큰 간 환전` \> `설정` \> `허용 슬리피지 (변동률)` 에서 정합니다.

기본값인 자동은 모든 견적을 받아 보고 고르는 방식입니다. 자동을 끄고 값을 직접 정하면 그보다 변동률이 큰 업체는 견적에 나타나지 않으며, 값을 너무 낮게 잡으면 견적을 아예 받지 못할 수 있습니다.

## 환전 설정

`토큰 간 환전` \> `설정` 에서 견적을 받는 기준을 바꿀 수 있습니다.

| 항목 | 내용 |
| --- | --- |
| 시세 표시 통화 | 견적 금액을 어느 통화로 환산해 보여줄지 정합니다. |
| 견적 정렬 기준 | 견적을 최저 비용순으로 볼지, 최고 속도순으로 볼지 정합니다. |
| 허용 슬리피지 (변동률) | 감수할 변동률의 상한을 정합니다. |
| 견적 업체 | 견적을 받을 업체를 고릅니다. 기본값은 전체이며, 업체명으로 검색해 일부만 선택할 수도 있습니다. |